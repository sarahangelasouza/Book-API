$kind: collection
name: Book API
description: A RESTful API for managing a library book collection. Supports
  listing, retrieving, creating, updating, and deleting books.
variables:
  baseUrl: "{{baseUrl}}"
