$kind: http-example
request:
  url: "{{baseUrl}}/books/:id"
  method: GET
  pathVariables:
    id: "{{bookID}}"
response:
  statusCode: 404
  statusText: Not Found
  body:
    type: json
    content: |-
      {
          "message": "Book with id {{bookId}} not found"
      }
order: 3487980809594596
