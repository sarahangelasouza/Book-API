$kind: http-request
description: Updates an existing book by ID. Fields `title`, `author`, `year` are all optional.
url: "{{baseUrl}}/books/:id"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  id: "{{bookID}}"
body:
  type: json
  content: |-
    {
      "title": "The Great Gatsby (Updated)",
      "author": "F. Scott Fitzgerald",
      "year": 1925
    }
order: 4000
