$kind: http-request
description: Activates the subscription.
url: "{{base_url}}/v1/billing/subscriptions/:subscription_id/activate"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  subscription_id: "{{subscription_id}}"
body:
  type: json
  content: |-
    {
        "reason": "Items are back in stock"
    }
examples: ./.resources/Activate subscription.resources/examples
order: 6000
